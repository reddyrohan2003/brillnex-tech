/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['smell-splendor-dramatic.ngrok-free.dev'],
  typescript: {
    ignoreBuildErrors: true,
  },
};

export default nextConfig;
